# Dependencies
node_modules/

# Build output (never commit compiled artifacts — no CI-bot commits)
dist/
build/
*.tsbuildinfo
out/
.next/
.turbo/

# Test / coverage
coverage/
.nyc_output/

# Vercel / env
.vercel/
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.idea/
*.swp
.vscode/*
!.vscode/extensions.json
.cache/

# Mappings regen review artifacts — transient output of `regen.mjs`. Promote a
# candidate to the committed map with `cp`, then delete it; never commit these.
*.candidate.json

# ─────────────────────────────────────────────────────────────
# NEVER commit the PolyTrack game or deobfuscated source.
# Legal posture: ship only our code + mappings metadata.
# ─────────────────────────────────────────────────────────────
/game/
/game-bundles/
*.bundle.js
polytrack_physics.wasm
ammo.wasm.*
deobfuscated/

# ─────────────────────────────────────────────────────────────
# Private research. Kept on disk, never published.
#
# The PML analysis notes live here: they read another project's source and
# scope its weaknesses, which is fine as working notes and not something to
# publish under our name. `docs/concepts/pml-compatibility.md` is the public
# statement of what the adapter does and why.
# ─────────────────────────────────────────────────────────────
docs/private/
