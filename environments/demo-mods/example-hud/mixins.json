{
  "$comment": "Mixins declared by the demo-hud mod. This patch targets the STABLE NAME 'Car' (M5-C: mappings-resolved, fail-closed) instead of an inline minified anchor — the mod is decoupled from the build's minification. The portal resolves 'Car' -> the Car module factory via @tspml/mappings, then applies this `after` op (injects a visible marker).",
  "patches": [
    {
      "op": "after",
      "symbol": "Car",
      "inject": "(function(){try{if(typeof window!=='undefined'){window.__demoHudMixin=true;if(typeof document!=='undefined'&&!document.getElementById('tspml-demo-hud-mixin')){var m=document.createElement('div');m.id='tspml-demo-hud-mixin';m.textContent='demo-hud mixin (stable name)';m.style.cssText='position:fixed;top:0;right:0;z-index:2147483647;background:#1a0a2e;color:#c792ea;font:600 13px ui-monospace,Menlo,monospace;padding:6px 10px;border-bottom-left-radius:6px;pointer-events:none';(document.body||document.documentElement).appendChild(m);}}}catch(e){}})();"
    }
  ]
}
