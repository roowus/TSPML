{
  "$comment": "Tspml Checkpoint Counter mixin(s). Target STABLE NAMES (mappings-resolved, fail-closed) or inline anchors. See docs/api/mixin-reference.md.",
  "patches": [
    {
      "op": "after",
      "symbol": "Car",
      "inject": "(function(){try{if(typeof window!=='undefined'){window.__tspml-checkpoint-counterMixin=true;}}catch(e){}})();"
    }
  ]
}
