{
  "name": "tspml",
  "version": "0.0.0",
  "private": true,
  "description": "TSPML — The Second Poly Mod Loader: a Fabric-inspired mod loader for PolyTrack.",
  "license": "MIT",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "docs:check": "echo \"docs site lands in a later milestone\""
  },
  "workspaces": [
    "source/*",
    "tooling/*",
    "environments/*",
    "packages/*"
  ]
}
