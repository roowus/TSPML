{
  "_comment": [
    "The TSPML mod catalog. A CURATED LIST, not a search index: there is no",
    "backend behind it, so there are no ratings, no download counts, and no",
    "version history. The UI says so where it is browsed.",
    "",
    "Read through lib/registry.ts, which validates every row and DROPS anything",
    "malformed rather than defaulting it. tests/registry.test.ts validates this",
    "file in CI, so a bad commit fails before it deploys.",
    "",
    "source.url may be relative. It resolves against whatever origin serves this",
    "file, which is why a portal-served entry would be relative — but the entries",
    "below are hosted by their authors (GitHub raw), so they install identically",
    "from local dev and production. A hardcoded tspml.vercel.app URL here would",
    "make a local dev server install from production and look like it worked.",
    "",
    "'format' is required and reserved: 'tspml' installs, 'pml' renders as a named",
    "refusal rather than being installed wrong. Adding an entry is a PR.",
    "",
    "The portal's own /sample-mod, /sample-mod-b and /sample-pack.txt fixtures",
    "are deliberately NOT catalog entries. They exist for the Playwright smokes,",
    "which need same-origin URLs that cannot rot with someone else's repo; a",
    "catalog listing them would advertise demo content to players."
  ],
  "schemaVersion": 1,
  "entries": [
    {
      "kind": "mod",
      "format": "tspml",
      "id": "poly-to-track",
      "name": "Poly To Track",
      "author": "roowus",
      "summary": "Import 3D files (STL/OBJ) — colored models supported — and build them as PolyTrack block builds: position a viewport ghost with Blender-style transform handles, then apply. Schematica for PolyTrack.",
      "tags": ["building", "editor", "models"],
      "icon": "https://raw.githubusercontent.com/roowus/poly-to-track/main/assets/icon.svg",
      "source": { "type": "mod-json", "url": "https://raw.githubusercontent.com/roowus/poly-to-track/main/mod.json" },
      "homepage": "https://github.com/roowus/poly-to-track",
      "gameVersions": [">=0.6.0 <0.7.0"],
      "safety": { "touchesPhysics": false, "leaderboardRisk": "none" },
      "dependencies": []
    }
  ]
}
